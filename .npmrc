# npm version's default commit message is just the raw version number
# (e.g. "1.0.1"), which fails the conventional-pre-commit commit-msg
# hook (see .pre-commit-config.yaml). This makes it Conventional
# Commits-compliant instead. %s is replaced with the new version.
message=chore(release): %s
