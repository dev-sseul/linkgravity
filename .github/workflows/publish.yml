name: Publish to npm

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"

      # npm 12 raised its own Node engine requirement above what
      # node-version 20 (above) provides, so @latest now breaks this step.
      # Pinned to the v11 major, which still satisfies OIDC's v11.5.1+ need.
      - name: Update npm (pinned to v11 for OIDC)
        run: npm install -g npm@11

      - name: Publish
        run: npm publish

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
